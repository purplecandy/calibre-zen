<?xml version="1.0" encoding="utf-8"?>
<!--
  The MSIX manifest for calibre-zen. package.ps1 fills the @FIELDS@ from
  packaging/upstream.json and packaging/windows/msix.json before packing.

  A full-trust desktop application: calibre spawns worker processes, opens
  named pipes and talks to devices over USB, none of which fits the app
  container, and runFullTrust is what every classic desktop app in the Store
  declares. The Store signs the package on submission, so nothing here names a
  certificate.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap10="http://schemas.microsoft.com/appx/manifest/uap/windows10/10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap uap10 rescap">

  <Identity
    Name="@IDENTITY_NAME@"
    Publisher="@PUBLISHER@"
    Version="@VERSION@"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>@DISPLAY_NAME@</DisplayName>
    <PublisherDisplayName>@PUBLISHER_DISPLAY_NAME@</PublisherDisplayName>
    <Description>@DESCRIPTION@</Description>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="calibrezen" Executable="calibre-zen.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="@DISPLAY_NAME@"
        Description="@DESCRIPTION@"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile
          Wide310x150Logo="Assets\Wide310x150Logo.png"
          Square71x71Logo="Assets\Square71x71Logo.png"
          Square310x310Logo="Assets\Square310x310Logo.png" />
      </uap:VisualElements>
      <Extensions>
        <!-- Opening a book from Explorer adds it to the library, as calibre does. -->
        <uap:Extension Category="windows.fileTypeAssociation">
          <uap:FileTypeAssociation Name="ebook">
            <uap:DisplayName>E-book</uap:DisplayName>
            <uap:SupportedFileTypes>
              <uap:FileType>.epub</uap:FileType>
              <uap:FileType>.kepub</uap:FileType>
              <uap:FileType>.mobi</uap:FileType>
              <uap:FileType>.azw</uap:FileType>
              <uap:FileType>.azw3</uap:FileType>
              <uap:FileType>.fb2</uap:FileType>
              <uap:FileType>.lit</uap:FileType>
              <uap:FileType>.lrf</uap:FileType>
            </uap:SupportedFileTypes>
          </uap:FileTypeAssociation>
        </uap:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
